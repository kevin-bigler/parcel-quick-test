{
  "name": "parcel-quick-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/index.html # good for development. live reload etc",
    "build": "npx parcel build src/index.html # produces static content",
    "serve": "npx http-server dist/ --port 1234 # serves static contents of dist/ folder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "devDependencies": {
    "http-server": "^13.0.2"
  }
}
